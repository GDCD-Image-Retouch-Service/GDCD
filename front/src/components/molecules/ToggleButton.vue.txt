<template>
  <div
    id="toggle-button-outline"
    @click="toglleImageCamera"
  >
    <!-- 이미지 -->
    <div
      id="image-camera"
      :style="{ color: red }"
    >
      <v-icon> mdi-image </v-icon>
    </div>
    <!-- 카메라 -->
    <div
      id="image-camera"
      :style="{ color: red }"
    >
      <v-icon> mdi-camera </v-icon>
    </div>

    <!-- 움직이는 버튼 -->
    <div id="toggle-button"></div>
  </div>
</template>

<script setup>
// import { useMainStore } from '@/store/index.js'

// const mainStore = useMainStore();

// const toglleImageCamera = () => {
//   if (mainStore.toggleTranslate === 'translateX(0)') {
//     mainStore.toggleTranslate = 'translateX(100%)'
//     mainStore.imageColor = 'var(--white-background)'
//     mainStore.cameraColor = 'var(--black)'
//   }
//   else {
//     mainStore.toggleTranslate = 'translateX(0)'
//     mainStore.imageColor = 'var(--black)'
//     mainStore.cameraColor = 'var(--white-background)'
//   }
// }

const toglleImageCamera = () => {
  console.log('버튼 입력');
};
</script>

<style scoped>
#toggle-button-outline {
  width: calc(100% - var(--grid));
  height: 40px;
  background-color: #595959;
  border-radius: 25px;
  margin: 0 auto;
  display: flex;
  position: relative;
}
#image-camera {
  position: absolute;
  top: calc(20px - 15px);
  left: calc(75% - 12px);
  z-index: 2;
  /* color: var(--white-background);  */
}
#image-camera:first-child {
  left: calc(25% - 12px);
}
#toggle-button {
  /* display: none; */
  background-color: var(--white);
  width: 50%;
  height: 100%;
  border: 1px solid #595959;
  border-radius: 25px;
  transition-duration: 0.3s;
}
</style>
